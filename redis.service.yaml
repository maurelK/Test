apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: bernstein
  labels:
    app: redis
    component: cache
    part-of: bernstein
spec:
  type: ClusterIP
  selector:
    app: redis
    component: cache
  ports:
  - name: redis
    protocol: TCP
    port: 6379
    targetPort: 6379
