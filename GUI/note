port:4242
host:127.0.0.1

./bin/zappy_server -p 4242 -x 40 -y 20 -n bot1 bot2 -c 2 -f 10 --auto-start on --display-eggs true

mv ../backup-zappy/* .

donc en faite on a deux choix soit on mais une imageen background(image d’une grande title) et on fait des sfrenctangle en noir(qui sera l’equivalent d’un segmant de title) ou soit on cherche un sprite tile qu’on add comment tu as fait mais il a un probleme  avec la deuxieme etape et c’est le taille des tile s sur une map plus grand les tiles seront plus petit a cause de leur normbre mais sur une map plus petit les tiles seront bcp plus grande donc on doit etre en mesure d’apater la taille des tiles en fonction de celle de la map je ne sais pas si tu vois
