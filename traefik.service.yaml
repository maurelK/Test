apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: kube-public
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 80
  - name: admin
    protocol: TCP
    port: 8080
    targetPort: 8080