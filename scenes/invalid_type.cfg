# Example invalid_type.cfg to trigger SettingTypeException
camera = {
    resolution = { width = 800; height = 600; };
    position = { x = 0.0; y = 0.0; z = 0.0; };
    rotation = { x = 0.0; y = 0.0; z = 0.0; };
    fieldOfView = "invalid_string";  # This should be a number, but is a string to cause type error
};

primitives = {
    spheres = (
        {
            center = { x = 0.0; y = 0.0; z = 0.0; };
            radius = 1.0;
        }
    );
};

lights = {
    ambient = 0.1;
};
